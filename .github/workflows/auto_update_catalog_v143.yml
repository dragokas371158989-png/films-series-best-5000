# Disabled by GKM V235 rollback
# Old V144 auto update can overwrite current site state.
name: DISABLED - auto_update_catalog_v143

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled old V144 auto update by GKM V235 rollback."
