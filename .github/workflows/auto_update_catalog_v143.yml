name: DISABLED Auto update catalog V144 Kinopoisk only

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in V248 stable restore to prevent old auto catalog overwrites."
