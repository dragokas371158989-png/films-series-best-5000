const DATA_URL="movies_updates.json";const PAGE_SIZE=40;const MIN_VOTES_FOR_TOP=300;let allMovies=[],filtered=[],currentPage=1,currentTab="all",selectedMovie=null;const $=id=>document.getElementById(id);const favKey="filmsSeriesFav",historyKey="filmsSeriesHistory";function loadSet(key){try{return new Set(JSON.parse(localStorage.getItem(key)||"[]"))}catch{return new Set()}}function saveSet(key,set){localStorage.setItem(key,JSON.stringify([...set]))}function titleOf(m){return m.ru||m.en||"Без названия"}function queryOf(m){return encodeURIComponent(titleOf(m))}function normalize(s){return String(s||"").toLowerCase().trim()}function getYear(m){return Number(m.year||0)}function getRating(m){return Number(m.rating||0)}function getVotes(m){return Number(m.votes||m.vote_count||m.rating_votes||0)}function getGenres(m){return Array.isArray(m.genres)?m.genres.filter(Boolean):[]}function isMovie(m){return m.type==="Фильм"}function isSeries(m){return m.type==="Сериал"}function isCartoon(m){const g=getGenres(m).join(" ").toLowerCase();const t=[m.ru,m.en,m.type].join(" ").toLowerCase();return g.includes("мульт")||g.includes("animation")||t.includes("мульт")}function isAnime(m){const hay=normalize([m.ru,m.en,m.type,...getGenres(m),m.studio,m.overview].join(" "));return hay.includes("аниме")||hay.includes("anime")||hay.includes("manga")||hay.includes("ova")}function smartScore(m){const rating=getRating(m),votes=getVotes(m),year=getYear(m);const votesScore=Math.log10(Math.max(votes,1))*1.35;const ratingScore=rating*1.9;const freshScore=year?Math.max(0,Math.min(1.2,(year-1990)/40)):0;const lowVotesPenalty=votes<100?4.5:votes<300?2.3:votes<1000?.8:0;return ratingScore+votesScore+freshScore-lowVotesPenalty}function sortList(list){const sort=$("sortFilter").value||"smart";const a=[...list];if(currentTab==="random")return shuffle(a);if(sort==="rating")return a.sort((x,y)=>getRating(y)-getRating(x)||getVotes(y)-getVotes(x));if(sort==="votes")return a.sort((x,y)=>getVotes(y)-getVotes(x)||getRating(y)-getRating(x));if(sort==="year")return a.sort((x,y)=>getYear(y)-getYear(x)||smartScore(y)-smartScore(x));if(sort==="title")return a.sort((x,y)=>titleOf(x).localeCompare(titleOf(y),"ru"));return a.sort((x,y)=>smartScore(y)-smartScore(x))}async function loadData(){ $("statusText").textContent="Загрузка movies_updates.json...";const res=await fetch(DATA_URL+"?v="+Date.now(),{cache:"no-store"});if(!res.ok)throw new Error("Не удалось загрузить movies_updates.json: "+res.status);const data=await res.json();allMovies=data.movies||data.items||[];$("statusText").textContent=`База: ${allMovies.length} записей · версия ${data.version||"?"} · ${data.generatedAt||""}`;fillFilters();applyFilters()}function fillFilters(){const years=[...new Set(allMovies.map(getYear).filter(Boolean))].sort((a,b)=>b-a);const genres=[...new Set(allMovies.flatMap(getGenres))].sort((a,b)=>a.localeCompare(b,"ru"));$("yearFilter").innerHTML='<option value="">Все годы</option>'+years.map(y=>`<option value="${y}">${y}</option>`).join("");$("genreFilter").innerHTML='<option value="">Все жанры</option>'+genres.map(g=>`<option value="${escapeHtml(g)}">${escapeHtml(g)}</option>`).join("")}function applyFilters(){const q=normalize($("searchInput").value),type=$("typeFilter").value,genre=$("genreFilter").value,year=Number($("yearFilter").value||0),minRating=Number($("ratingFilter").value||0);let list=[...allMovies];if(currentTab==="movies")list=list.filter(isMovie);else if(currentTab==="series")list=list.filter(isSeries);else if(currentTab==="cartoons")list=list.filter(isCartoon);else if(currentTab==="anime")list=list.filter(isAnime);else if(currentTab==="top")list=list.filter(m=>getVotes(m)>=MIN_VOTES_FOR_TOP).sort((a,b)=>smartScore(b)-smartScore(a)).slice(0,250);else if(currentTab==="new"){const maxYear=Math.max(...allMovies.map(getYear).filter(Boolean));list=list.filter(m=>getYear(m)>=maxYear-1)}else if(currentTab==="popular")list=list.filter(m=>getVotes(m)>=500).sort((a,b)=>getVotes(b)-getVotes(a)).slice(0,500);else if(currentTab==="fav"){const fav=loadSet(favKey);list=list.filter(m=>fav.has(String(m.id)))}else if(currentTab==="history"){const hist=JSON.parse(localStorage.getItem(historyKey)||"[]");const map=new Map(allMovies.map(m=>[String(m.id),m]));list=hist.map(id=>map.get(id)).filter(Boolean)}else if(currentTab==="random")list=shuffle(list).slice(0,200);if(q)list=list.filter(m=>normalize([m.ru,m.en,m.year,m.type,m.status,m.overview,m.studio,...getGenres(m)].join(" ")).includes(q));if(type)list=list.filter(m=>m.type===type);if(genre)list=list.filter(m=>getGenres(m).includes(genre));if(year)list=list.filter(m=>getYear(m)===year);if(minRating)list=list.filter(m=>getRating(m)>=minRating);if(!["history"].includes(currentTab))list=sortList(list);filtered=list;currentPage=1;render()}function render(){const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));currentPage=Math.min(currentPage,pages);const start=(currentPage-1)*PAGE_SIZE;const pageItems=filtered.slice(start,start+PAGE_SIZE);$("countText").textContent=`Найдено: ${filtered.length} · Страница ${currentPage} из ${pages}`;$("grid").innerHTML=pageItems.map(cardHtml).join("");document.querySelectorAll(".card").forEach(card=>{card.addEventListener("click",()=>{const id=card.getAttribute("data-id");const movie=allMovies.find(m=>String(m.id)===id);if(movie)openDetails(movie)})});$("prevBtn").disabled=currentPage<=1;$("nextBtn").disabled=currentPage>=pages;$("pageText").textContent=`${currentPage} / ${pages}`}function cardHtml(m){const poster=m.poster?`<img loading="lazy" src="${escapeAttr(m.poster)}" alt="${escapeAttr(titleOf(m))}">`:`<div class="no-poster">Нет постера</div>`;const genres=getGenres(m).slice(0,3).join(" · ");const votes=getVotes(m),rating=getRating(m);const badges=[votes>=1000?'<span class="badge good">проверенный топ</span>':"",getYear(m)>=2025?'<span class="badge warn">новинка</span>':"",isCartoon(m)?'<span class="badge">мульт</span>':""].join("");return `<article class="card" data-id="${escapeAttr(m.id)}"><div class="poster-wrap"><div class="badges">${badges}</div>${poster}</div><div class="card-body"><p class="card-title">${escapeHtml(titleOf(m))}</p><p class="meta">${escapeHtml(m.year||"—")} · ${escapeHtml(m.type||"—")}</p><p class="meta">${escapeHtml(genres)}</p><span class="rating">${rating.toFixed(1)}</span><span class="votes">${votes?formatNum(votes)+" голосов":""}</span></div></article>`}function openDetails(m){selectedMovie=m;const hist=JSON.parse(localStorage.getItem(historyKey)||"[]").filter(id=>id!==String(m.id));hist.unshift(String(m.id));localStorage.setItem(historyKey,JSON.stringify(hist.slice(0,300)));$("detailTitle").textContent=titleOf(m);$("detailMeta").textContent=`${m.year||"—"} · ${m.type||"—"} · рейтинг ${getRating(m).toFixed(1)} · голосов ${getVotes(m)||0}`;$("detailGenres").textContent=getGenres(m).join(" · ");$("detailOverview").textContent=m.overview||"Описание пока не добавлено.";$("detailPoster").src=m.poster||"";$("detailPoster").style.display=m.poster?"block":"none";const q=queryOf(m);$("kinopoiskLink").href=`https://www.kinopoisk.ru/index.php?kp_query=${q}`;$("youtubeLink").href=`https://www.youtube.com/results?search_query=${q}+трейлер`;$("vkLink").href=`https://vk.com/video?q=${q}`;$("rutubeLink").href=`https://rutube.ru/search/?query=${q}`;$("googleLink").href=`https://www.google.com/search?q=${q}+фильм+сериал`;updateFavBtn();$("detailsDialog").showModal()}function updateFavBtn(){const fav=loadSet(favKey);const yes=selectedMovie&&fav.has(String(selectedMovie.id));$("favBtn").textContent=yes?"Убрать из избранного":"В избранное"}function toggleFav(){if(!selectedMovie)return;const fav=loadSet(favKey);const id=String(selectedMovie.id);if(fav.has(id))fav.delete(id);else fav.add(id);saveSet(favKey,fav);updateFavBtn();if(currentTab==="fav")applyFilters()}function shuffle(arr){const a=[...arr];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}function formatNum(n){if(!n)return"";if(n>=1000000)return(n/1000000).toFixed(1)+"м";if(n>=1000)return Math.round(n/1000)+"к";return String(n)}function escapeHtml(s){return String(s??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[ch]))}function escapeAttr(s){return escapeHtml(s)}function setupEvents(){["searchInput","typeFilter","genreFilter","yearFilter","ratingFilter","sortFilter"].forEach(id=>{$(id).addEventListener("input",applyFilters);$(id).addEventListener("change",applyFilters)});document.querySelectorAll(".tab").forEach(btn=>{btn.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));btn.classList.add("active");currentTab=btn.dataset.tab;applyFilters()})});$("prevBtn").addEventListener("click",()=>{if(currentPage>1){currentPage--;render();window.scrollTo({top:0,behavior:"smooth"})}});$("nextBtn").addEventListener("click",()=>{const pages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));if(currentPage<pages){currentPage++;render();window.scrollTo({top:0,behavior:"smooth"})}});$("closeDialog").addEventListener("click",()=>$("detailsDialog").close());$("favBtn").addEventListener("click",toggleFav);$("reloadBtn").addEventListener("click",()=>loadData().catch(showError));$("themeBtn").addEventListener("click",()=>{document.body.classList.toggle("light");localStorage.setItem("filmsTheme",document.body.classList.contains("light")?"light":"dark")});if(localStorage.getItem("filmsTheme")==="light")document.body.classList.add("light")}function showError(e){console.error(e);$("statusText").textContent="Ошибка: "+e.message;$("grid").innerHTML='<div class="card"><div class="card-body">Не удалось загрузить базу. Проверь, что рядом лежит movies_updates.json.</div></div>'}setupEvents();loadData().catch(showError);
